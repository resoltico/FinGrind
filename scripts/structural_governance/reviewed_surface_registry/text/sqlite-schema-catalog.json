{
  "relativePath": "sqlite/src/main/resources/dev/erst/fingrind/sqlite/book_schema.sql",
  "owner": "sqlite-schema",
  "reason": "The canonical SQLite schema is one atomically applied bootstrap catalog whose ordered statements and trigger bodies define the protected-book fingerprint.",
  "splitTrigger": "Introduce generated schema composition only when the runtime can preserve one canonical ordered fingerprint while independently owning a reusable schema family; do not split this bootstrap catalog mechanically.",
  "reviewedRoleName": "sqlite-schema-catalog",
  "budgetVarianceReason": "The atomic schema catalog exceeds the default SQLite SQL budget because bootstrap and fingerprinting require its ordered DDL and trigger text to remain in one canonical file.",
  "approval": {
    "physicalLines": 3703,
    "logicalLines": 3562,
    "importLikeLines": 160,
    "functions": 0,
    "nestedTypes": 0,
    "expiresOn": "2026-10-01"
  }
}
