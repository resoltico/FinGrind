package dev.erst.fingrind.contract;

/** Presence classification for one template field inside a contract-owned shape policy. */
enum ContractTemplateFieldPresence {
  REQUIRED,
  OPTIONAL,
  FORBIDDEN
}
