# FinGrind container assembly is built from the staged Docker build context emitted by
# `./gradlew :cli:stageDockerBuildContext`, not from the repository root.
# Repository-root `docker build .` is intentionally unsupported so the sender cannot walk
# arbitrary checkout-local state or sibling build outputs.
**
